<p>
  <strong>User:</strong>
  <%= @calendar.user_id %>
</p>

<p>
  <strong>Name:</strong>
  <%= @calendar.name %>
</p>

<%= @calendar.description %>

<%= link_to 'Edit', edit_calendar_path(@calendar) %> |
<%= link_to 'Back', :back %>
